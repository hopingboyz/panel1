<!DOCTYPE html>
<html class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %> - Register</title>
  <link href="../assets/tailwind.css" rel="stylesheet">
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
</head>
<body class="bg-[#101010] h-full text-white font-sans">
<div class="min-h-screen flex items-center justify-center px-6">
  <div class="bg-[#181818] p-8 rounded-2xl shadow-lg max-w-md w-full">
    <% if (settings.logo) { %>
      <div class="flex justify-center mb-6">
        <img class="h-12 w-auto" src="<%= settings.logo %>" alt="Logo">
      </div>
    <% } %>
    <h2 class="text-center text-2xl font-semibold">Create an Account</h2>
    <p class="text-center text-sm text-gray-400 mt-2">Join us by filling in the details below.</p>
    
    <form method="POST" action="/auth/register" class="mt-6 space-y-4">
      <div>
        <label class="block text-sm font-medium text-gray-300">Username</label>
        <input type="text" name="username" class="w-full mt-1 p-3 bg-[#222] border border-gray-700 rounded-xl placeholder-gray-500 focus:ring focus:ring-blue-500" placeholder="example" required>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-300">Email</label>
        <input type="email" name="email" class="w-full mt-1 p-3 bg-[#222] border border-gray-700 rounded-xl placeholder-gray-500 focus:ring focus:ring-blue-500" placeholder="example@domain.com" required>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-300">Password</label>
        <input type="password" name="password" class="w-full mt-1 p-3 bg-[#222] border border-gray-700 rounded-xl placeholder-gray-500 focus:ring focus:ring-blue-500" placeholder="********" required>
      </div>
      
      <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-black font-semibold py-3 rounded-xl transition">Register</button>
    </form>
    
    <% if (req.query.err) { %>
      <p class="text-center text-red-500 mt-4 text-sm">Registration failed. Please try again.</p>
    <% } %>
    
    <p class="text-center text-sm text-gray-400 mt-4">Already have an account? <a href="/login" class="text-blue-400 hover:text-blue-300">Sign in</a></p>
  </div>
</div>
</body>
</html>
